= simple_form_for @speak, :html => { :class => 'form-horizontal' } do |f|
  = f.input :text, input_html: {rows: 5}
  .form-actions
    = f.button :submit, :class => 'btn btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), speaks_path, :class => 'btn'
